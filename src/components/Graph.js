import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  border: 1px solid var(--gray);
`;

const Graph = () => {
  return <Container>graph</Container>;
};

export default Graph;
